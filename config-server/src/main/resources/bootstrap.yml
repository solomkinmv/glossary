spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
        git:
          uri: http://github.com/solomkinmv/glossary
          search-paths: /configs

server:
  port: 8888
